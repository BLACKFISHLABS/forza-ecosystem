# APPLICATION
spring:
  application:
    name: Forza
    description: Projeto desenvolvido por BLACKFISH LABS.
    version: @project.version@
  profiles:
    active: test
  jpa:
    properties:
      hibernate:
        id:
          new_generator_mappings: true

# SERVER
server:
  contextPath: /
  port: 5000